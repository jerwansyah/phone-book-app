import { css } from '@emotion/react'

export const shadow2px = css({
  background: 'var(--neutral-white, #FFF)',
  boxShadow: '0px 2px 4px 0px rgba(171, 190, 209, 0.60)'
})

export const shadow4px = css({
  background: 'var(--neutral-white, #FFF)',
  boxShadow: '0px 4px 8px 0px rgba(171, 190, 209, 0.40)'
})

export const shadow8px = css({
  background: 'var(--neutral-white, #FFF)',
  boxShadow: '0px 8px 16px 0px rgba(171, 190, 209, 0.40); '
})
